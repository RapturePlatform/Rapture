coord = {};
coord['id'] = "test";

println("Waiting on " + coord);

resp = wait(coord, 10, 10);

if resp != null do
    println("we have response");
    return true;
else do
	println("no response");
end
