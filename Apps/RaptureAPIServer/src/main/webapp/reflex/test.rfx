// This is a Reflex script that generates output
aDoc = {};
aDoc['test'] = 'two';

println(json(aDoc));

println("------");

// And now for the parameter map

println(json(web));

workflows = #workflow.getWorkflows();

println(json(workflows));