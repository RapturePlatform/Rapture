returnVal = false;

//---------- test -----------
//NUMBER LISTS


emptyList = [];
fullList = emptyList + 4;

fullList = fullList + 1 + 2;

fullList = fullList - 4;

println ('Test existence in list after remove');
if (1 in fullList) do
	returnVal = true;
else do
	returnVal = false;
end

if (!(4 in fullList)) do
	returnVal = true;
else do
	returnVal = false;
end
assert(returnVal);
